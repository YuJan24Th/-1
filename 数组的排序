#include<stdio.h>
int main（）{
int a，b，arr[5]，tmp;
for（int i=0;i<=4，i++）{
scanf（“%d”，&arr[i]）;
   }
for（a=0;a<=3;a++）{
for（b=a+1;b<=4;b++）
if（arr[a]<arr[b]）{
tmp=arr[a];
arr[a]=arr[b];
arr[b]=tmp;
         }
   }
}
for（int i=0;i<=4;i++）{
printf（“%d”，arr[i]）;
}
